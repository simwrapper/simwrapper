export interface InitParams {
  accessToken: string
  fileId: string
  projectId: string
}

/*
export interface PlanRequestParams {
  idIndex: number
}
*/

export enum MethodNames {
  FetchXML = 'FetchXML',
}
